# User-Manager-Service-Frontend
The **SPA User Manager Service Frontend** is a modern web application built with **React.js**, **TypeScript**, and **Redux**, providing a seamless interface for managing user data. It integrates with the **User-Management-Service** backend project, allowing users to view, add, edit, and delete user information.
